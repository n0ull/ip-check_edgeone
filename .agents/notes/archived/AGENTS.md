# AGENTS.md — 归档笔记

`archived/` 分类目录下的笔记三元组（本项目为单语 `.md`）是**冻结的历史快照**，不是当前权威。
永远不要编辑、重排、翻译、修复、删除或移动已封存的文件；新决策与新事实使用活跃笔记或当前文档。

归档变更仅允许：移动笔记到 `archived/{class}/`、在 `Status: implemented` 下方插入 `Archived: YYYY-MM-DD` 行、修复或删除入链。
不检查、不验证、不修复归档笔记的出链。归档元数据由 `npm run verify:notes` 校验。