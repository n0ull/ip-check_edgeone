# IP 查询服务

EdgeOne Makers 边缘函数上的轻量 IP 回显服务领域术语表。决策与实现细节见 [.agents/notes/](.agents/notes/README.md)，端点与用法见 [README.md](README.md)。

## Language

**强制 IPv4**：
`4.` 子域的站点语义：平台侧关闭该域名的 IPv6 接入，任何连接必以 IPv4 到达，函数必返回访问者 IPv4。
_Avoid_: IPv4 优先（「优先」描述双栈竞争，`4.` 上不存在竞争）

**双栈判定**：
`test.` 子域的站点语义：返回本次连接实际使用的 IP；结果为 IPv6 即「IPv6 访问优先」，该地址即访问者的 IPv6。

**派生**：
IPv6 地址不独立获取，一律从双栈判定结果推出——平台不存在「仅 IPv6」站点状态。

**页面脚本作用域**：
一个页面的全部浏览器端函数与其胶水语句嵌套其中的真实函数；词法作用域即闭包边界，也是序列化的单位。
_Avoid_: 序列化数组

**序列化数组**（已退役）：
2026-08-29 前手工枚举页面函数的数组；已被页面脚本作用域的自动闭包取代。阅读旧笔记时仅作历史概念。
